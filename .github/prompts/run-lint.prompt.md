---
mode: agent
description: sprawdź kod linterem ruff i zgłoś problemy
---

Uruchom `ruff check .` oraz `ruff format --check .` w katalogu głównym projektu.
Wypisz wykryte problemy i zaproponuj poprawki. Jeśli chodzi tylko o
formatowanie, możesz je naprawić poleceniem `ruff format .`.
